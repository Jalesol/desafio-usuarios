import { Routes } from '@angular/router';
import { UsersListComponent } from './features/users/pages/users-list/user-list';

export const routes: Routes = [
  { path: '', component: UsersListComponent }
];